extern crate guessing_game;

mod tests {
  // I'm assuming the io will need some mocking/stubbing,
  // so these tests need to wait.
  // There doesn't seem to be any info online about testing
  // simple `println!`s.
  // Was hoping to at least test the `cmp` function, but
  // I'd need to mock the Ordering module...
}
